cmake_minimum_required(VERSION 3.0)
project(temporalgradient)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS}, inc)
add_executable(temporalgradient src/main.cpp)
target_link_libraries(temporalgradient ${OpenCV_LIBS})